import React from "react";
import DashboardLayout from "../../../layouts/dashboard/index";
import ReferContent from "../composition/ReferContent";

const ReferAndEarn = () => {
  return <DashboardLayout content={<ReferContent />} />;
};

export default ReferAndEarn;
